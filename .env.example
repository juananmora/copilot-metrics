# Frontend Environment Variables

# Enable WebSocket mode for real-time updates
# Set to 'true' to use WebSocket, 'false' for traditional polling
VITE_USE_WEBSOCKET=false

# WebSocket server URL (only needed if different from default)
# Default: ws://localhost:3001/ws (proxied through Vite in dev)
VITE_WS_URL=ws://localhost:3001/ws
